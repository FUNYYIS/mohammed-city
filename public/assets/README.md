# Runtime assets

Only approved, documented game assets belong here. Every imported asset must have a matching entry in `docs/ASSET_LICENSES.md` and the TypeScript `AssetRegistry`.

The expected final player path is:

`characters/mohammed/mohammed.glb`

Empty category directories are intentional during Phase 1; no placeholder file is presented as final art.
